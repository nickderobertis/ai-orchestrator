# Python
__pycache__/
*.py[cod]
.venv/
# uv
# (uv.lock IS committed)

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
test-results/
playwright-report/
# The run directory the DAG UI browser fixture server rebuilds on every start.
/apps/dag-ui/e2e/.fixture/
.mypy_cache/
.ruff_cache/

node_modules/
.nx/
dist/

# Editor / OS
.DS_Store
*.swp

# Orchestrator run artifacts (reports written with -o land wherever the caller
# points; keep any local scratch out of the tree)
/scratch/
/runs/
