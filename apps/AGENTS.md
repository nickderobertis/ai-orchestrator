# Applications

Applications are deployable user interfaces and servers. Keep transport models
at their trust boundary, consume shared packages rather than importing another
app, and provide accessible labels, roles, focus order, and keyboard paths.
Every app project exposes `build`, `lint`, `test`, and `typecheck` Nx targets.
