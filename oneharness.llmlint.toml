# llmlint: ignore-file[changed_behavior_has_e2e] config parsing plus the real llmlint wrapper boundary (scripts/llmlint-oneharness.sh) prove this chain without spending another paid model invocation.
# LLM lint is a third, independent side of the harness, with its own priority
# order — it keeps that order even though the worker's discovered oneharness.toml
# prefers Claude Code. Forced by scripts/llmlint-oneharness.sh.
#
# It extends `oneharness.identities.toml` rather than `oneharness.dispatch.toml`: the
# judged tier is run by a recipe rather than started by the engine, so there is no node
# scratch directory for the runtime-directory repoint to point at. A worker's own
# `just lint-llm-diff` runs it inside a dispatch, but the recipe is the caller either
# way.
extends = "oneharness.identities.toml"

# The judged tier is run by a recipe rather than started by the engine, so nothing
# else switches recording on for it and no `onepipeline.*` label is inherited unless a
# worker's own `just lint-llm-diff` is what invoked it — in which case the dispatch's
# keys come through the worker's environment and `role = "llmlint"` is what tells this
# tier's sessions from that worker's own. The key is this host's; the engine stamps none
# like it.
history_labels = { role = "llmlint" }

# Codex carries this tier, and a second Codex identity absorbs an exhausted quota
# before the chain leaves the provider. Past that it now reaches the workers'
# alternate Claude subscriptions before the primary one — this tier is no longer
# Codex-only. That is the operator's deliberate trade: a blocking pre-push check
# that can still be judged beats one isolated from the workers' Claude quota.
harnesses = [
    "codex:primary",
    "codex:alternate",
    "claude-code:alternate",
    "claude-code:alternate2",
    "claude-code:primary-backup",
    "claude-code:primary",
]

# Codex's read-only filesystem sandbox is usable on this host, but its default
# network isolation asks bubblewrap to configure loopback in a namespace that the
# outer container forbids. Grant network only: Codex retains its OS-enforced
# read-only filesystem while avoiding that unsupported namespace operation.
#
# This lives here, per harness, rather than as the `-- <HARNESS_ARG>` passthrough
# scripts/llmlint-oneharness.sh used to append: oneharness appends those trailing
# arguments to WHICHEVER harness fallback selects, and now that this chain reaches
# Claude Code, `-c` there is claude's `--continue` boolean — so the permission
# string would be swallowed as a positional prompt, silently replacing the lint
# prompt with junk instead of failing. A `[harness.codex]` entry cannot reach a
# non-Codex candidate at all, and both Codex identities inherit it. It is only
# consulted when Codex actually sandboxes (this tier forces oneharness
# `--mode read-only`); with the sandbox bypassed there is no policy to widen.
#
# Declared at the harness level, which is also what keeps this role's variants
# untouched — and so keeps every credential mask `oneharness.identities.toml` states
# for them. `oneharness.dispatch.toml` explains why mentioning a variant at all is
# what costs a mask on the released oneharness.
# llmlint: ignore[least_privilege_grants] Codex exposes only coarse disk/network grants; llmlint needs repository reads and its model endpoint, with the outer container as boundary.
[harness.codex]
args = ["-c", 'sandbox_permissions=["disk-full-read-access","network-full-access"]']

# The board credential is the plan store's and this role never reaches the store, so it
# stops here rather than travelling to the provider. Stated per role rather than in the
# shared parent because it is a per-role policy and a parent's mask is one no child can
# remove — `oneharness.identities.toml` says so at this same variant. The board
# *nomination* is left travelling on purpose: a live lane handed the owner, number and
# repository but no token skips and says why, where one handed nothing cannot tell a
# misconfigured lane from a deliberate one.
[harness.codex.variant.primary]
unset_env = ["GH_PROJECTS_TOKEN"]
