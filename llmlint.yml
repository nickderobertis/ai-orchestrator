# yaml-language-server: $schema=https://raw.githubusercontent.com/nickderobertis/llmlint/main/assets/llmlint.schema.json
#
# The LLM-judge tier — separate from the deterministic `just check` gate.
# Run with `just lint-llm` (or `just lint-llm-diff` for the merge-base
# diff); the diff-scoped run is the blocking PR check, not part of `check`.
# Rules come from the pinned plugins below; tune one in place with
# `override: true`. Bump a plugin's `@version` pin to pull new rules.
files:
  # No `include`: llmlint lints the whole tree (exclude + .gitignore honored).
  # List committed files that shouldn't be judged (lock files, generated output).
  exclude:
    - "**/.git/**"
rationales: true
# Findings and broken judge runs are not cached by the `lint-llm-diff` tier — Nx
# caches successful tasks only — so a branch working through a red re-rolls the
# judge each time, and `llmlint history` is the only durable account of what each
# roll said. The default 100 records is one afternoon of that on a busy branch.
history:
  # llmlint: ignore[changed_behavior_has_e2e] A retention count is a logic-free config value read by llmlint itself, not behavior this repository implements; an e2e for it could only assert that llmlint honors its own documented setting.
  max_runs: 200
# No `agents` block: scripts/llmlint-oneharness.sh pins this tier to the dedicated
# oneharness.llmlint.toml. It names the same five identities every role does, but
# keeps its own priority order rather than inheriting the worker's
# alternate-Claude-first chain.
plugins:
  - "https://raw.githubusercontent.com/nickderobertis/llmlint/main/assets/config_lint.yml@1"
  - "https://raw.githubusercontent.com/nickderobertis/dero-skills/main/skills/bootstrap/create-repo/assets/llmlint/base.llmlint.yml@1"
  - "https://raw.githubusercontent.com/nickderobertis/dero-skills/main/skills/bootstrap/create-repo/assets/llmlint/project-graph.llmlint.yml@1"
  - "https://raw.githubusercontent.com/nickderobertis/dero-skills/main/skills/bootstrap/create-repo/assets/llmlint/languages/python.llmlint.yml@1"
  - "https://raw.githubusercontent.com/nickderobertis/dero-skills/main/skills/bootstrap/create-repo/assets/llmlint/languages/bash.llmlint.yml@1"
  - "https://raw.githubusercontent.com/nickderobertis/dero-skills/main/skills/bootstrap/create-repo/assets/llmlint/ci.llmlint.yml@1"

# Statically disable a plugin rule that cannot apply to this repo. This is a
# synchronous CLI orchestration harness: every module shells out to git/gh/onejudge via
# subprocess by design (and the e2e suite drives real git/subprocess), with no async
# server context and no typed client for an external process. `relevance: false` marks
# `async_typed_clients_at_boundaries` not-relevant repo-wide instead of re-suppressing it
# per file across every module it touches.
rules:
  - name: orchestration_contract_changes_update_docs
    relevance: >-
      The diff changes observable planner/operator behavior in the just command
      surface, planner-orchestrator channel or live-edit protocol, graph round or
      replan semantics, status/exit behavior, or documented repo-lifecycle policy.
    description: >-
      The diff updates the corresponding durable operator documentation in
      AGENTS.md, docs/orchestration.md, or docs/repo-lifecycle.md.
    files:
      include:
        - "justfile"
        - "orchestrator/channel.py"
        - "orchestrator/edits.py"
        - "orchestrator/graph.py"
        - "orchestrator/plan.py"
        - "orchestrator/next_round.py"
        - "orchestrator/replan.py"
        - "orchestrator/runs.py"
        - "orchestrator/lifecycle.py"
        - "orchestrator/recover.py"
        - "orchestrator/merge.py"
        - "orchestrator/registry.py"
        - "AGENTS.md"
        - "docs/orchestration.md"
        - "docs/repo-lifecycle.md"
  - name: async_typed_clients_at_boundaries
    override: true
    relevance: false
  # Nx project files and the root justfile are declarative command adapters:
  # `just check` drives every non-mutating quality target for real. The three check
  # scripts likewise have real success and semantic-failure journeys; injecting
  # host failures such as broken mktemp/git/mkdir belongs to their explicit guards,
  # not e2e. Recursively invoking bootstrap, format, upgrade, or the full check
  # from its own pytest process would mutate the active environment and cannot end.
  #
  # `config/*.version` is the adopted release of a published CLI, and bumping one
  # exposes that CLI's behavior rather than adding any this repository implements.
  # Each engine is proven in its own repository — its own gate and its own e2e
  # suite — so an e2e demanded here could only be a second, weaker copy of a proof
  # that already ran upstream. What *is* this repository's to prove at that
  # boundary is recipe delegation and the journeys built on it, which
  # `e2e_not_mocked` judges against real `just`, `onevcs`, and git.
  - name: changed_behavior_has_e2e
    override: true
    files:
      exclude:
        - "justfile"
        - "**/project.json"
        - "config/*.version"
        # These four and `config/*.version` above are five faces of one dependency
        # pin: a bump is written as a manifest line and mechanically restated in that
        # manifest's lockfile, so one change reaches this rule at up to five sites.
        # None of them is application source — a pin adopts behaviour rather than
        # adding any this repository implements, which is the reasoning
        # `config/*.version` was already excluded on, and excluding one face while
        # leaving the others is the inconsistency rather than the fix. What a bundle
        # bump adopts also cannot be exercised from here: a release state renders only
        # from a release event, a run carries one only where a repository declares a
        # release target, and no repository registered on this host declares one. What
        # *is* provable is proven — `tests/e2e/test_dag_ui_serving_e2e.py` drives the
        # adopted bundle through a real browser and asserts the empty release row
        # deliberately, so the absence is gated rather than merely unproven. The
        # exclusion is per-rule, so every other rule still judges these files
        # normally: `pyproject.toml`'s coverage floor in particular stays gated by
        # `tests/test_coverage_gate.py`, which none of this touches. Revisit the whole
        # set the day a release target is declared here, when that data becomes
        # suppliable.
        - "package.json"
        - "bun.lock"
        - "uv.lock"
        - "pyproject.toml"
        - "scripts/check-dag-state-contract.py"
        - "scripts/check-nx-cache.sh"
        - "scripts/check-oneharness-ui-contract.sh"
  # Tests read suite-controlled environment variables set by the suite itself,
  # not inputs crossing an external trust boundary.
  - name: boundary_inputs_validated
    override: true
    files:
      include:
        - "orchestrator/**"
        - "scripts/**"
