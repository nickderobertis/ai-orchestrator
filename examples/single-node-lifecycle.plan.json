{
  "schema_version": 1,
  "goal": {
    "text": "Give the service a health endpoint operators can poll"
  },
  "name": "health-endpoint",
  "tasks": [
    {
      "id": "health",
      "repo": "nickderobertis/some-service",
      "repo_type": "team",
      "persona": "engineer",
      "title": "feat(api): add /health endpoint",
      "task": "## What\nAdd a `GET /health` endpoint that returns build metadata, with tests that drive real requests.\n\n## Why\nOperators currently learn a deploy is unhealthy from user reports, because nothing exposes build metadata to poll.\n\n## Acceptance criteria\n- `GET /health` returns the build metadata contract.\n- Real request tests prove the happy path and one failure path.\n- Each new test is observed failing for the intended reason before it passes.",
      "done_when": "all task acceptance criteria are met, real requests prove the endpoint without opening coverage gaps, and the repository's full gate passes"
    }
  ]
}
