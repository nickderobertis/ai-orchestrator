{
  "schema_version": 6,
  "concurrency": 4,
  "tasks": [
    {
      "id": "api",
      "repo": "nickderobertis/some-service",
      "repo_type": "team",
      "persona": "engineer",
      "task": "Add a POST /health endpoint that returns build metadata, with tests that drive real requests. Keep the change scoped to the endpoint.",
      "done_when": "real requests prove the endpoint response and validation behavior, and the repository's full gate passes",
      "title": "feat(api): add /health endpoint"
    },
    {
      "id": "docs",
      "repo": "nickderobertis/some-docs",
      "repo_type": "team",
      "merge_policy": "auto",
      "persona": "docs-writer",
      "task": "Document the new /health endpoint (request/response, examples). Verify every example against the API.",
      "title": "docs(api): document /health"
    },
    {
      "id": "client",
      "repo": "nickderobertis/some-client",
      "repo_type": "team",
      "persona": "engineer",
      "task": "Add a typed health() method to the client that calls GET /health, with a test against a real local server.",
      "deps": ["api"],
      "title": "feat(client): add health() method"
    },
    {
      "id": "dashboard",
      "repo": "nickderobertis/some-service",
      "repo_type": "team",
      "deps": ["api"],
      "title": "feat(admin): add dashboard",
      "steps": [
        {
          "id": "impl",
          "persona": "engineer",
          "task": "Implement a read-only /admin dashboard page that surfaces the health metadata, with tests that render the page and drive real requests.",
          "done_when": "the rendered dashboard exposes every health field through real requests, access control is covered, and the full gate passes"
        },
        {
          "id": "approval",
          "kind": "human",
          "task": "Exercise the dashboard in staging and approve final review.",
          "deps": ["impl"]
        }
      ]
    }
  ]
}
