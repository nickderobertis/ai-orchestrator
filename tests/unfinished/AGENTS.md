<!-- llmlint: ignore-file[instruction_layer_localized] `.github/CODEOWNERS` has a wildcard owner for this subtree; another entry would not change ownership. -->
# `tests/unfinished`

The journeys over `just unfinished`: the real recipe, wrappers and modules over a scratch
`onevcs` registry of labelled sessions and a built runs root. Keep in-process tests of the
module in the orchestrator coverage tier. Before adding a file to `unfinishedWorkspace`,
confirm the tier reads it; a broad glob would charge every unrelated edit for these real
session journeys.
