{
  "name": "run-end-hooks",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "tests/run_end_hooks",
  "tags": [
    "scope:run-end-hooks",
    "type:tests"
  ],
  "targets": {
    "test": {
      "command": "env -u FORCE_COLOR NO_COLOR=1 uv run pytest tests/run_end_hooks --no-cov -n 4 --dist loadgroup",
      "options": {
        "cwd": "."
      },
      "inputs": [
        "runEndHooksWorkspace"
      ]
    }
  }
}
