{
  "name": "plan-store-install",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "tests/plan_store_install",
  "tags": [
    "scope:plan-store-install",
    "type:tests"
  ],
  "targets": {
    "test": {
      "command": "env -u FORCE_COLOR NO_COLOR=1 uv run pytest tests/plan_store_install --no-cov -n 4 --dist loadgroup",
      "options": {
        "cwd": "."
      },
      "inputs": [
        "planStoreInstallWorkspace"
      ]
    }
  }
}
