# `tests/merge_policy`

The journeys that hold the `merge_policy` vocabulary this repository's prose restates to
the one the installed launcher accepts, by launching a real `just orchestrate` per policy
and reading its refusal.

- **One target, `merge-policy:test`, keyed on `mergePolicyWorkspace`**, which names
  files rather than trees: every journey here spends a real launch, so every path in the
  key they never read makes an unrelated edit pay for that. The key is the launcher's
  own reads plus the three documents these tests hold to it, which is why a prose-only
  diff selects this project and no other project of its kind.
