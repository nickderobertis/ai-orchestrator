{
  "name": "ask-seam-channel-reply",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "tests/ask_seam/channel_reply",
  "tags": [
    "scope:ask-seam",
    "type:tests"
  ],
  "targets": {
    "test": {
      "command": "env -u FORCE_COLOR NO_COLOR=1 uv run pytest tests/ask_seam/channel_reply --no-cov -n 4 --dist loadgroup",
      "options": {
        "cwd": "."
      },
      "inputs": [
        "askSeamChannelReply"
      ]
    }
  }
}
