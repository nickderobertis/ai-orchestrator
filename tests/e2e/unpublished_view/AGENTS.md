<!-- llmlint: ignore-file[instruction_layer_localized] `.github/CODEOWNERS` has a wildcard owner for this subtree; another entry would not change ownership. -->
# `tests/e2e/unpublished_view`

Keep in-process view tests in the orchestrator coverage tier. Put a test here when it
needs the real recipe, wrapper, or pinned producer source. Before adding a file to this
project's Nx key, confirm the test reads it; a broad glob would charge every unrelated
edit for these real session journeys.
